# JWT encryption keys.
JWT_SECRET_LONGLIVED=Some unique string, preferably generated by running this in a Node shell and saving its output as this value: `require('crypto').randomBytes(64).toString('hex')`. Should be different to the below secret.
JWT_SECRET_SHORTLIVED=Some unique string, preferably generated by running this in a Node shell and saving its output as this value: `require('crypto').randomBytes(64).toString('hex')`. Should be different to the above secret.

# Discord OAuth2
DISCORD_CLIENT_ID=Get via the Discord Developer Portal under the OAuth2 section of an application: https://discord.com/developers/applications 
DISCORD_CLIENT_SECRET=Get via the Discord Developer Portal under the OAuth2 section of an application: https://discord.com/developers/applications 
OAUTH_REDIRECT_DISCORD=Route for production to handle OAuth success responses, such as: https://auth.bigfootds.dev/users/discord/redirect
OAUTH_REDIRECT_DISCORD_DEV=Route for dev/test to handle OAuth success responses, such as: http://localhost:3030/users/discord/redirect

# Twitch OAuth2
TWITCH_CLIENT_ID=Get via the Twitch Developer Console under the "Manage Application" section of an app: https://dev.twitch.tv/console/apps 
TWITCH_CLIENT_SECRET=Get via the Twitch Developer Console under the "Manage Application" section of an app: https://dev.twitch.tv/console/apps 
OAUTH_REDIRECT_TWITCH=Route for production to handle OAuth success responses, such as: https://auth.bigfootds.dev/users/twitch/redirect
OAUTH_REDIRECT_TWITCH_DEV=Route for dev/test to handle OAuth success responses, such as: http://localhost:3030/users/twitch/redirect

# MongoDB/Mongoose NoSQL database
DATABASE_URL=MongoDB Cloud Atlas database connection string specified under "Connect" and then "Connect your application" in the "Database Deployments" section of the dashboard. Connection string should also include the above username and password. It should look like this: mongodb+srv://<username>:<password>@<database name>.<some mongodb.net domain name>/?retryWrites=true&w=majority

# Postmark email credentials
NOREPLY_EMAIL_ADDRESS=Whatever email you configure as a "Sender Signature" in PostmarkApp.com. https://account.postmarkapp.com/signature_domains
POSTMARK_API_TOKEN=Server API token from the "API Tokens" page of a server set up on PostmarkApp.com. https://account.postmarkapp.com/servers/<your server ID>/credentials